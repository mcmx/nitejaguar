package web

templ HelloForm() {
    @WorkflowsPage(&WorkflowPageData{}, "")
}

// HelloPost is retained for compatibility with generated callers.
templ HelloPost(name string) {
    <div>{ name }</div>
}
